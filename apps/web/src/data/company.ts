/**
 * The currently active company.
 *
 * Hardcoded placeholder for now. When the multi-company switcher is built, this
 * becomes the selected company from the backend — replace usages with that
 * source. Kept in one place so the header and pages stay in sync.
 */
export const CURRENT_COMPANY_NAME = 'TechNova Solutions';
